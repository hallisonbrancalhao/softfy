import { Route } from '@angular/router';
import { FeatureHomeContainer } from './feature-home.container';

export const featureHomeRoutes: Route[] = [
  {
    path: '',
    component: FeatureHomeContainer
  }
];
